/**
 * DEPRECATED — do not run against production.
 * Admins already exist (Cloudflare Access / prior setup).
 */
console.error(
  "make_admin.mjs is deprecated for this project. Admins already exist — aborting.",
);
process.exit(1);
